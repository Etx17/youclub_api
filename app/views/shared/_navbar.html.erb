<div class=" px-3 d-flex justify-content-between align-items-center" style="height: 60px">
  <%= link_to "#", class: "navbar-brand" do %>
    <span class="bangers-font">Youclub.</span>
  <% end %>

  <% if controller_name == 'pages' %>
    <p class="text-secondary"><span class="text-success"><%= 200 - Lead.count %></span> places restantes</p>
  <% end %>
  <!--<div class="dropdown">
    <button class="btn btn-info dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
      <%= controller_name.humanize %>
    </button>
    <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
      <% if controller_name != 'activities' %>
        <%= link_to 'Activités', activities_path, class: 'dropdown-item' %>
      <% end %>
      <% if controller_name != 'clubs' %>
        <%= link_to 'Clubs', clubs_path, class: 'dropdown-item' %>
      <% end %>
    </ul>
  </div> -->

  <% if controller_name != 'pages' %>
  <%= form_tag update_zipcode_path, method: :get, class:"d-flex border justify-content-center gap-2", style: "max-width: 40%;" do %>
      <%= text_field_tag :zipcode, session[:zipcode] || nil, style: "width: 50%" %>
      <%= submit_tag "Ok", class: "btn gradient-button shadow-sm w-50" %>
    <% end %>

  <div class="d-flex align-items-center">
    <%= link_to '#', data: { bs_toggle: "modal", bs_target: "#locationModal" } do %>
      <i class="fas fa-map-marker mx-3 text-secondary"></i>
    <% end %>
    <%= link_to '#', class: "ml-2" do %>
      <i class="fas fa-bars text-secondary"></i>
    <% end %>
  </div>
  <% end %>
</div>
